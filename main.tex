\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
% The preceding line is only needed to identify funding in the first footnote. If that is unneeded, please comment it out.
%Template version as of 6/27/2024

% \usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{array}
\usepackage{multirow}
\usepackage[
backend=biber,
style=numeric,
]{biblatex}
\addbibresource{../refs.bib}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
% Zotero citation key:
%     auth.lower+year+title
\usepackage{hyperref}
\begin{document}

% A Multi-Modal Approach To Human Action Anticipation
\title{Learning Early Visual Motion Cues From Pose-Anchored Optical Flow For Human Action Anticipation
% \thanks{Funding provided by the Australian Government and Swinburne University of Technology.}
}

\author{\IEEEauthorblockN{Anonymous ICME Submission}}
% \IEEEauthorblockN{2\textsuperscript{nd} Given Name Surname}
% \IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
% \textit{name of organization (of Aff.)}\\
% City, Country \\
% email address or ORCID}
% \and
% \IEEEauthorblockN{3\textsuperscript{rd} Given Name Surname}
% \IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
% \textit{name of organization (of Aff.)}\\
% City, Country \\
% email address or ORCID}
% \and
% \IEEEauthorblockN{4\textsuperscript{th} Given Name Surname}
% \IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
% \textit{name of organization (of Aff.)}\\
% City, Country \\
% email address or ORCID}
% \and
% \IEEEauthorblockN{5\textsuperscript{th} Given Name Surname}
% \IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
% \textit{name of organization (of Aff.)}\\
% City, Country \\
% email address or ORCID}
% \and
% \IEEEauthorblockN{6\textsuperscript{th} Given Name Surname}
% \IEEEauthorblockA{\textit{dept. name of organization (of Aff.)} \\
% \textit{name of organization (of Aff.)}\\
% City, Country \\
% email address or ORCID}
% }

\maketitle

\begin{abstract}
  This paper proposes a simple modification that can be made to existing human skeleton action recognition models to improve action classification accuracy and human action prediction capability.
  % What?
  We propose a method of extracting human early visual motion cues by sampling windows of optical flow surrounding skeleton keypoints in image space. We have titled this Pose-guided Optical Flow Feature methodology, PoseOFF.
  % So what?
  Windows of optical flow sampled surrounding human pose keypoints embed motion information in the learning process and enable the recognition of actions with fewer observed frames.
  % Now what?
  % Three state-of-the-art skeleton-based action recognition models, (InfoGCN++, MS-G3D and ST-GCN++) are modified with a CNN embedding layer and show improvements in action classification and action prediction accuracy.
  With the addition of a lightweight embedding layer to learn windows of optical flow extracted with PoseOFF, three state of the art skeleton-based action recognition models (InfoGCN++, MS-G3D, ST-GCN++) show improvements in action classification accuracy and action prediction ability on three human action recognition datasets.
  % Testing the dataset on a non-skeleton specific dataset
  The three models were trained and evaluated on the NTU RGB+D 60, NTU RGB+D 120 skeleton datasets and the UCF101 RGB dataset.
  Evaluation of the models on an RGB only dataset demonstrate the broad applications of data extraction and embedding method.
  % Optical flow windows embed motion information in the learning process and enable the recognition of actions with fewer observed frames.
\end{abstract}

\begin{IEEEkeywords}
  Human Action Recognition, Online Action Recognition, Human Motion Prediction, Multi-modal Media Computing
\end{IEEEkeywords}


\section{Introduction}
% Why is human early action recognition important?
Human action prediction is a common task in computer vision that aims to predict actions in video data.
Where such predictions are required quickly and as part of a human responsive interaction loop, the problem becomes one of action anticipation, in which the aim is to accurately predict actions in as few observed frames as possible.
Example applications include human-robot teaming~\cite{yu2021MultisensorGestureInteractionSystemHumanrobotCooperation}, AR/VR applications~[CITE], safety monitoring in video surveillance~\cite{ryoo2011HumanactivitypredictionEarlyrecognitionongoingactivitiesstreamingvideos}, among others.
The task of human action prediction focuses on the accuracy, speed and frame latency of action classification~\cite{lai2024HumanActionAnticipationSurvey}.
In such cases, rapid low-latency action anticipation may allow interactive systems to pre-load resources such as pre-loading relevant objects in mixed reality (XR) systems, or pruning and/or priming potential response actions in human-machine interfacing contexts, supporting quicker, more natural interactions.

%  Short summary of optical flow, what it tells us
[Short summary of optical flow, what it tells us]

% Human early visual motion cues
Anticipatory action prediction is well established in the primate brain.
In particular, the role of early visual motion cues are well understood to offer predictive and response-priming cues during human movement and action [CITE].
In human vision, these motion cues are perceived at early stages of visual processing, made available from optical flow - the perceived movement of light patterns projecting onto the eye - to prime other centres of the brain (e.g., the pre-motor cortex)~\cite{meichow2023Eyemovementsopticflowperception}.
% The article cited below is really quite good, but I'm struggling to find anything that suggests that motion patters localised around skeleton points of interest...
Human vision research indicates that the human motor system plays a crucial role when observing and predicting other's actions \cite{gredeback2015EyeMovementsActionObservation}.
In primate brains, kinematic information of human skeletal interest points, typically joints, supports human action prediction, as well as emotional intent understanding.
% This sentence doesn't really flow on...
This motivates the consideration of how such kinematic information, as well as human early visual motion cues, can be used to support similar capabilities of visual interpretation of human movements in computer vision scenarios.

% Human skeleton based action recognition
Human skeleton keypoints are 2D or 3D locations of major body joints extracted from images or videos, providing a compact and interpretable representation of human pose in each frame.
However, skeleton keypoints are inherently static descriptors and do not capture the local motion context surrounding each joint.
Although tracking keypoints across frames yields displacement vectors, this point-based information omits important second-order motion characteristics—such as instantaneous accelerations, rotations, or subtle surface deformations—that are only evident in the surrounding optical flow field.
As a result, skeleton-only representations encode joint positions and coarse movements but miss the richer local dynamics that are critical for early action understanding.

For actions with similar gross body movements—such as distinguishing a handshake from a punch—keypoint-only models are unlikely to make accurate predictions until most of the action sequence has unfolded.
In contrast, keypoint-anchored visual motion cues (e.g., wrist acceleration, torso rotation or deformation) may provide early discriminative signals that support faster and more reliable action prediction.

% % Skeleton-approaches BENEFITS
% Human skeleton keypoints are 2D or 3D locations of major human body joints extracted from images or videos.
% These skeleton keypoints provide meaningful and succinct information about human actions.
% % Skeleton-based action recognition has grown as an area of interest in the last two decades.
% % The growing interest is partially due to the increase in availability of RGB-D sensors, giving rise to more skeleton-based action recognition datasets.
% % Skeleton keypoints contain  \textit{only} information about the position and movement of humans and ignore noisy background elements.
% Skeleton keypoints contain \textit{only} information about position and movement of humans.

% % Skeleton-approaches CHALLENGES
% While this allows action recognition algorithms to learn from data that is free from environmental noise, such as dynamic lighting conditions and background clutter, skeleton-only approaches fail to capture dynamic relationships between keypoints during action performance, and do not cope well with limb occlusion.
% Dedicated skeleton action recognition datasets often require human actions to be captured from multiple angles using multiple RGB-D sensors to produce reliable skeleton information~\cite{shahroudy2016NTURGB+DLargeScaleDataset3DHumanActivityAnalysis}~\cite{liu2020NTURGB+D120LargeScaleBenchmark3DHumanActivityUnderstanding}.
% For actions with similar body motions, skeleton-only learning approaches fail to capture contextual features that may differentiate the actions, such as actions like ``put on backpack'' and ``put on jacket''.

% Action anticipation
Action classification and action anticipation are two separate but related tasks.
In action classification, sometimes called offline action recognition, is retrospective recognition, where the full action has been completed and observed, and a label (or labels) are assigned.
Action anticipation is the prediction of an a label (or labels) before the action has fully unfolded, using a partial observation of the full action.

We are not the first to consider optical flow for the purpose of action recognition [REFS].
While others have considered the role of optical flow, no one, to the best of our knowledge has considered it's use specifically for capturing localised visual motion petterns anchored at skeletal key points.
Most unique about this work is that we are defining local regions of flow so as to allow prediction models to not just exploit the tracked spatial patterns in each point-anchored flow field, but to learn the relationship with other point anchored patches.
Not simply predicting trajectories
Exploit spatial motion patterns present at the onset of actions, which may provide anticipatory benefit.
% There has been some work that utilises flow for human action prediction [CITE], but a distinct lack of work that has sought to embed local point-anchored optical flow information into action prediction model for the purpose of capturing local motion patterns.


% % RGB approaches CHALLENGES
% RGB-only approaches to human action recognition must contend with large amounts of calculations needed to process video data.
% Complex environments, such as outdoor settings or shifting camera perspectives, present challenges for RGB-only models in capturing robust human action features~\cite{wang2014CrossviewActionModelingLearningRecognition}.
% RGB-only models designed to perform human early action recognition suffer when distinguishing intra-class differences and inter-class similarities.

% Why optical flow and why PoseOFF?
To address these challenges, we propose a method of embedding localised spatio-temporal features extracted from keypoint-anchored windows of optical flow in existing skeleton-based action recognition models.
Our Pose-guided Optical Flow Feature extraction method (PoseOFF) involves the strategic sampling of windows of optical flow surrounding human skeleton keypoints.
Optical flow can provide more nuanced information than the simple displacement vectors of skeleton keypoints.
A simple embedding layer integrated in three state-of-the-art skeleton-based action recognition algorithms shows an improvement in classification accuracy and action prediction.

% PoseOFF extraction method diagram
\begin{figure*}[ht]
  \centering
  \includegraphics[width=0.9\linewidth]{figures/Flowpose_diagram_HORIZONTAL_HUG.png}
  \caption{\label{fig:poseoff_extraction}Extraction method for sampling windows of optical flow surrounding human pose keypoints. From the RGB videos, both human skeleton keypoints (pose) and optical flow are extracted. Windows of optical flow vectors are sampled surrounding each human pose keypoint to generate Pose-guided Optical Flow Feautres (PoseOFF). This PoseOFF data includes both joint coordinates in (x,y,z) space, as well as 25 (u,v) vectors representing the window of optical flow sampled surrounding each pose keypoint.}
\end{figure*}

% Contributions
The main contributions of this work are:
\begin{itemize}
  % Sampling flow around pose keypoints is a way to extract early visual motion cues
  \item PoseOFF, a novel feature embedding derived from optical to encode local spatial visual motion cues for early human action prediction from early visual motion cues.
  % Simple embedding layer
  \item A novel embedding layer that can be easily implemented in existing skeleton-based action recognition models to learn windows of optical flow.
  % Demonstration of increased accuracy
  \item The first demonstration that embedding early visual motion cues at pose-anchored locations can support reduced latency action predictions at comparable accuracy. We show this for three state-of-the-art skeleton-based action recognition models, using three established action prediction datasets.
  % Demonstration of increased action prediction
  \item Novel insights guiding the practical implementation of our proposed approach within real-time vision processing pipelines to support responsive and robust action anticipation.
\end{itemize}

% How is this paper structure?
This paper is structured as follows. \autoref{sec:related_work} presents a short literature review on action prediction and state-of-the-art skeleton action recognition models, \autoref{sec:method} outlines the PoseOFF extraction and embedding pipeline, along with it's practical implementation, \autoref{sec:experiments} compares the baseline human action classification and action prediction results against those obtained using PoseOFF embedded models, and \autoref{sec:conclusion} concludes the paper and presents opportunities for future research.



\section{Related work}%
\label{sec:related_work}
  % Important recent methods
  % ``In this work, we focus specifically on methdos that employ skeleton keypoints''
  % Visual motion in action prediction
  % Biological/human vision systems
  % A couple of recent examples of flow being used for action prediction in artificial systems
  % Summary remarks: ``While action prediction methods have progressed significantly, and methods involving flow have been previously explored, there is little work exploring more targeted uses of spatial patterns of flow at keypoints to capture anticipatory cues.''

% Important recent methods
Action classification and anticipation are long standing problems in computer vision, with increasing interest in approaches that can support real-time and online action prediction.
In this work, we focus specifically on methods that employ skeleton keypoints for these tasks.
The nature of the 2D or 3D points that represent human skeleton keypoints lends itself well to graph-based learning approaches.
% Skeleton-action recognition...
State-of-the-art skeleton-based human action recognition algorithms, such as ST-GCN++~\cite{duan2022PYSKLGoodPracticesSkeletonActionRecognition} and MS-G3D~\cite{liu2020DisentanglingUnifyingGraphConvolutionsSkeletonBasedActionRecognition} utilise Graph Convolutional Network (GCN) architecture to learn to classify human actions in spatio-temporal skeleton sequences.
These models are designed to perform human action classification; making a prediction after viewing the complete action sequence.


% Action anticipation
To approach the problem of human action anticipation using human skeleton keypoints, Chi et al.~\cite{chi2023InfoGCNLearningRepresentationPredictingFutureOnlineHumanSkeletonbasedActionRecognition} incorporated a Neural Ordinary Differential Equations module into a skeleton-based action recognition model to predict future skeleton states.
Their model, InfoGCN++, was able to classify human actions in skeleton datasets with fewer observed frames than existing skeleton-based action recognition models.

% Visual motion in action prediction
Skeleton-based approaches to human action classification and anticipation neglect local dynamics present in RGB videos that may provide discriminative signals about the nature of a human action before it's conclusion.
The efficacy of visual motion cues for early action prediction is supported by biological vision research.
% Biological vision examples of fast action prediction
In many ball-sports, it is nearly impossible for the human eye to accurately track the trajectory of the ball.
A meta-analysis~\cite{mann2007PerceptualCognitiveExpertiseSportMetaAnalysis} of eye-tracking research in sporting contexts indicates that elite athletes direct their gaze to information-dense areas.
Football goalkeepers that received perception training on areas of a penalty taker's body to focus on showed an increase performance when anticipating the direction of the penalty taker's shot~\cite{savelsbergh2010Anticipationpenaltykickingdirectioncanbeimproveddirectingattentionperceptuallearning}.
% In social situations, humans fixate primarily on the torso, and self report that their attention focuses on hands, elbows and feet~\cite{ennisEyetracktiveMeasuringattentionbodypartswhenjudginghumanmotions}.

% Flow for human action recognition and action anticipation
Using this motion information [CITE] demonstrated that human actions could be anticipated with fewer frames that using standard RGB videos.
Cai et al.~\cite{cai2019TemporalHockeyActionRecognitionPoseOpticalFlows} proposed a two-stream architecture for hockey action recognition that utilised both optical flow and human poses.
The inclusion of an optical flow stream increased hockey action classification accuracy by 10\%, demonstrated the complementary features presented in optical flow and human pose.

% Papers containing flow as learnable features, possibly showing how it can enhance action prediction ability...
https://openaccess.thecvf.com/content_ICCV_2019/papers/Gammulle_Predicting_the_Future_A_Jointly_Learnt_Model_for_Action_Anticipation_ICCV_2019_paper.pdf
https://arxiv.org/pdf/1912.07148
https://openaccess.thecvf.com/content_ICCV_2019/papers/Furnari_What_Would_You_Expect_Anticipating_Egocentric_Actions_With_Rolling-Unrolling_LSTMs_ICCV_2019_paper.pdf

% No work using targeted uses of spatial patterns of flow @ keypoints
While action prediction methods have progressed significantly, and methods involving flow have been previously explored, there is little work exploring more targeted uses of spatial patterns of flow at keypoints to capture anticipatory cues~\cite{lai2024HumanActionAnticipationSurvey}.


\section{Method}%
\label{sec:method}
We propose a novel feature extraction method designed to be easily integrated within existing state of the art skeleton-based action recognition models.
The feature extraction methodology, PoseOFF, is designed to sample information about human early visual motion cues.

By embedding these early motion cues as learnable features in skeleton-based action recognition models, we aim to show that the features obtained through the PoseOFF extraction method can provide these state-of-the-art models comparable accuracy with fewer observed frames. Moreover, we seek to achieve higher action anticipation ability with minimal additional computational overheads, thus retaining the feasibility of the methods applied in real-time settings on standard computing hardware.

\subsection{PoseOFF}%
\label{subsec:PoseOFF}
% What is PoseOFF
Pose-guided Optical Flow Features, PoseOFF, is a feature embedding for the task of human action prediction.
PoseOFF aims to capture localised spatio-temporal cues at the onset of human actions, which may be observable from the local deformation of optical flow fields.
We have chosen to sample these spatio-temporal cues surrounding human-pose keypoints.
The choice of sampling these areas in videos is well aligned with vision science literature, which has shown that human perception of other human movement is primed by human motion cues in these key areas in order to react and respond in a timely manner.
Given our application area seeks to support similar capabilities, it makes sense to explore how this may be utilised in an artificial vision system.
% We have chosen to sample optical flow surrounding human-pose keypoints as these are areas in videos of humans where motion relating to human actions take place (the human body).
% Specifically, optical flow surrounding human pose keypoints was chosen as a way to embed human early visual motion cue information into existing skeleton action recognition models.

% Two types of data extracted with PoseOFF
To obtain PoseOFF features for a video containing a human action, two types of data are extracted from videos; human skeleton keypoints (poses) and optical flow.
From each frame of video, human poses are estimated, producing a set of points within image space where key joints are located (for example, elbows, shoulders, hips, knees).
For each pair of temporally adjacent frames, optical flow is estimated, producing a vector field indicating the motion of the pixels between the adjacent frames.

% Sample windows of optical flow
Based on the estimated locations of skeleton keypoints, square windows are samples from the optical flow vector field.
Human skeleton action recognition models represent the skeleton keypoints as a graph, with nodes of the graph containing information about the relative 2D or 3D positions of keypoints.
PoseOFF appends the windows of optical flow surrounding these keypoints as additional graph node information.
An overview of the data extraction approach is depicted in~\autoref{fig:poseoff_extraction}.

% The final output of PoseOFF
% Pose shapes
The input to the PoseOFF embedding extraction method is an RGB video of $T$ frames.
For each frame $t$, we extract up to two human skeletons $M$, consisting of $V$ keypoints.
Each skeleton keypoint contains $C_{\text{pose}}=3$ channels.
Depending on the human pose extraction method, these skeleton keypoint channels either represent $(x,y,z)$ coordinates, where the z coordinate is an estimation of the distance that keypoint is from the camera, or $(x,y,\text{conf.})$, where $\text{conf.}$ is a value representing the pose estimation model's detection confidence level.

% Flow shapes
For each pair of temporally adjacent frames in the RGB video, optical flow is estimated to produce a total of $T-1$ optical flow vector fields, with $(u,v)$ vectors at each pixel location representing the motion that pixel took to reach it's current position in the latter frame.
Centred at every skeleton keypoint, we sample $N\times N$ windows of optical flow vectors, producing a window of shape $(N,N,2)$.
Optical flow vectors may be sampled with dilation, where vectors are sampled at regular intervals set by a dilation factor to increase the receptive field.
Examples of dilated window sampling is presented in~\autoref{fig:dilations}.

Each optical flow feature window is flattened to produce $C_{\text{flow}}=2N^2$ channels.
These channels are appended to skeleton keypoint channels to produce a total of $C=C_{\text{pose}}+C_{\text{flow}}=2N^2+3$ channels for each skeleton keypoint.
The output of the PoseOFF embedding is a vector of shape:
\[ (T, M, V, C) \]

% % Sampling optical flow windows with dilation
% Based on our testing (not reported here), a window size of $5\times5$ flow vectors demonstrated a good balance between data efficiency and learnable features.
% For videos with higher resolutions, we sample optical flow vectors with a dilated window to increase the receptive field of the flow windows.
% A demonstration of two dilated windows is presented in~\ref{fig:dilations}.

% Dilation examples
\begin{figure}[ht]
  \centering
  \includegraphics[width=0.8\linewidth]{figures/Dilation/Dilations.png}
  \caption{\label{fig:dilations}Example of a $5\times5$ window of flow field sampling with dilation 1 (left) and dilation 3 (right). The grid represents the vector field of optical flow. The blue cell in the centre is the optical flow vector aligning with the pixel coordinates of the human pose keypoint around which the flow window is being sampled.}
\end{figure}

% PoseOFF Network diagram!
\begin{figure}[ht]
  \centering
  \includegraphics[width=0.7\linewidth]{figures/poseOFF_network_diagram/PoseOFF_network_diagram.png}
  \caption{\label{fig:embedding_network_diagram}The embedding layer that learns the windows of optical flow, and concatenates these learned features with the original pose features (x,y,z positions).}
\end{figure}

% Data efficiency of PoseOFF
PoseOFF sampling provides a considerably more data efficient method of embedding optical flow information that training a neural network to learn from the entire flow field.
\autoref{table:datasize_comparison} provides a comparison of data sizes required for pose only, pose with full optical flow, and PoseOFF for a single video containing 100 frames at a resolution of $1920\times1080$ pixels.
\begin{center}
  \begin{table}\centering
    \caption{\label{table:datasize_comparison}Approximate data size of different forms of data for video $1920\times1080$ pixel resolution video containing two bodies and 100 frames. We assign 25 pose keypoints per body. For simplicity, we assume all values are stored as float32 (4 bytes) per value.}
    \begin{tabular}{|c|c|c|}
      \hline
      Data type & Number of Samples & Data size \\
      \hline
      Pose only & 60,000 & 60kB \\
      PoseOFF & 1,060,000 & 1060kB \\
      Pose with full flow & 1,658,880,000 & 1.66GB \\
      \hline
    \end{tabular}
  \end{table}
\end{center}

% Why use flow and not just joint motion?
Optical flow provides richer information than joint displacement vectors alone.
Windows of optical flow can contain nuanced information about the rotation or deformation of surfaces surrounding keypoints, where displacement vectors only indicate joint positional translation~\cite{cai2019TemporalHockeyActionRecognitionPoseOpticalFlows}.
% Where displacement indicates where a joint has come from, windows of optical flow can contain a more nuanced motion information about the moving pixels~\cite{cai2019TemporalHockeyActionRecognitionPoseOpticalFlows}.
% PoseOFF presents a considerably more data efficient method of embedding this nuanced motion information into existing models.




\subsection{Skeleton-based Action Recognition Models}
\label{subsec:models}
% How we modified the neural networks
To demonstrate the applicability and effectiveness of PoseOFF, we modify three state-of-the-art skeleton-keypoint action recognition models to incorporate learnable early visual motion cues.
The embedding layer of each model was modified to include a simple CNN designed to learn the features present in the windows of optical.
The CNN consisted of two convolutional layers separated by activation layers, pooling and a linear layer.
The output of these layers was concatenated with the original skeleton keypoints coordinates as additional channels in the skeleton graph.
A diagram of the PoseOFF embedding layer added to each model is presented in \autoref{fig:embedding_network_diagram}.

% Which models were tested?
The models tested using this methodology were InfoGCN++~\cite{chi2023InfoGCNLearningRepresentationPredictingFutureOnlineHumanSkeletonbasedActionRecognition}, MS-G3D~\cite{liu2020DisentanglingUnifyingGraphConvolutionsSkeletonBasedActionRecognition}, and ST-GCN++~\cite{duan2022PYSKLGoodPracticesSkeletonActionRecognition}. The models were trained using the hyperparametes defined by the models' authors, with settings as close to those provided in the respective papers.


% Trained on joint only stream
The PoseOFF extraction method utilises human skeleton \textit{joints} as locations in image space from which to extract windows of optical flow.
For this reason, the results presented in this paper are models trained and evaluated using the \textit{skeleton joint stream only}.

\subsubsectionmark{Observation Ratio}\label{subsubsec:obs_ratio}
To test each model's performance at the task of early action recognition, the final frames of the input sequence were masked to simulate partial observation of the action.
Input sequences were masked at specific ratios - \textit{observation ratios} - according to the input shape of each model.
See [FIG-X] for an example of masked input sequence.
We used the temporal padding methods outlined in~\cite{chi2023InfoGCNLearningRepresentationPredictingFutureOnlineHumanSkeletonbasedActionRecognition},~\cite{liu2020DisentanglingUnifyingGraphConvolutionsSkeletonBasedActionRecognition}, and~\cite{duan2022PYSKLGoodPracticesSkeletonActionRecognition} to pad the partially observed sequences to the frame length for each model's input.


\subsection{Implementation}
\label{subsec:implementation}
% What algorithms did we use to calculate optical flow?
All models were implemented in Python using the PyTorch learning framework~\cite{paszkePyTorchImperativeStyleHighPerformanceDeepLearningLibrary}, using the code provided by their authors.
Optical flow was extracted using the PyTorch implementation of the RAFT algorithm~\cite{teed2020RAFTRecurrentAllPairsFieldTransformsOpticalFlow}.
When skeleton keypoints are not available, we extract human pose keypoints using the YOLO-POSE Large model~\cite{maji2022YOLOPoseEnhancingYOLOMultiPersonPoseEstimationUsingObjectKeypointSimilarityLoss}.

% Hyperparameter management?
The hyperparameters were chosen to match those defined by the model's authors for the NTU RGB+D 60 and NTU RGB+D 120 datasets.
For the UCF101 dataset, which did not have hyperparameters defined by the authors, we chose to mirror the settings used for the NTU RGB+D 60 dataset where possible.
Both the base model and PoseOFF embedded model were trained with identical hyperparameter settings.




\subsection{Datasets}
\label{subsec:datasets}
The models were trained and evaluated on three different datsets; the NTU RGB+D 60 dataset~\cite{shahroudy2016NTURGB+DLargeScaleDataset3DHumanActivityAnalysis},NTU RGB+D 120 dataset~\cite{liu2020NTURGB+D120LargeScaleBenchmark3DHumanActivityUnderstanding} and the UCF101 dataset~\cite{soomro2012UCF101dataset101humanactionsclassesvideoswild}.

% About the NTU dataset
The NTU RGB+D 60 and NTU RGB+D 120 datasets consist of images obtained from three separate Microsoft Kinect v2 sensors, with ground truth and contains 3D human skeleton data for every sample.
These datasets contain RGB videos, depth sequences, skeleton data (3D locations of 25 major body joints), and infrated frames.
Distinct train/test splits (evaluations) are provided by the authors in the respective papers for each dataset, ensuring all models that utilise these datasets are training and testing on the same samples to provide fair comparisons.
Samples in these datasets are assigned one class label from a possible 60 or 120 classes respectively.
Given the ubiquity of these datasets in the skeleton action recognition literature~\cite{chi2023InfoGCNLearningRepresentationPredictingFutureOnlineHumanSkeletonbasedActionRecognition}~\cite{liu2020DisentanglingUnifyingGraphConvolutionsSkeletonBasedActionRecognition}~\cite{duan2022PYSKLGoodPracticesSkeletonActionRecognition}, they were chosen to provide a baseline for comparing base model performance to a model trained with PoseOFF embeddings.

% About the UCF101 dataset
The UCF101 dataset is composed of human actions within unconstrained videos collected ``in the wild''.
This is an RGB only video dataset, and both the human skeleton keypoints and optical flow were estimated using the methods outlined in Sec.~\ref{subsec:implementation}.
The authors provide three distinct train/test splits.
This dataset was chosen as it provides a challenging skeleton action recognition task, using videos that were collected outside of laboratory conditions like those present in the NTU RGB+D datasets.

% Dilation factors between datasets
The RGB video data of the NTU RGB+D datasets has a resolution of $1920\times1080$ pixels, whereas the UCF101 dataset has a resolution of $320\times240$ pixels.
Windows of optical flow for the PoseOFF embedding were sampled with a dilation factor of 3 for the NTU RGB+D datasets and a dilation factor of 1 for the UCF101 datasets.
A higher dilation factor was chosen for the NTU RGB+D datasets given their higher video resolution.
See~\autoref{fig:dilations} an example of dilated window sampling.



% Classification accuracy table
\begin{center}
  \begin{table*}[ht!]\centering
    \caption{\label{table:classification_accuracy}Classification accuracy for all models trained and tested on the NTU-RGB+D (NTU), NTU-RGB+D120 (NTU120) and UCF101 datasets - joint stream only. The train/test splits (evaluations) for each dataset are presented in separate columns under each datasets heading.}
    \resizebox{.8\textwidth}{!}{
    \begin{tabular}{ll|ll|ll|lll|}
      \cline{3-9}
      &  & \multicolumn{2}{l|}{NTU} & \multicolumn{2}{l|}{NTU120} & \multicolumn{3}{l|}{UCF101} \\ \cline{3-9}
      &  & \multicolumn{1}{l|}{CS} & CV & \multicolumn{1}{l|}{CSub} & CSet & \multicolumn{1}{l|}{1} & \multicolumn{1}{l|}{2} & 3 \\ \hline

      \multicolumn{1}{|l|}{\multirow{2}{*}{InfoGCN++}} & Base & 83.52\% & 91.38\% & 75.92\% & 77.61\% & 59.21\% & 57.82\% & 58.90\% \\ \cline{2-2}
      \multicolumn{1}{|l|}{} & \textbf{PoseOFF} & \textbf{88.89\%} & \textbf{93.68\%} & \textbf{82.70\%} & \textbf{83.70\%} & \textbf{62.81\%} & \textbf{62.54\%} & \textbf{62.84\%} \\ \hline

      \multicolumn{1}{|l|}{\multirow{2}{*}{MS-G3D}} & Base & 87.40\% & 93.43\% & 76.24\% & 78.75\% & 61.39\% & 62.02\% & 63.45\% \\ \cline{2-2}
      \multicolumn{1}{|l|}{} & \textbf{PoseOFF} & \textbf{89.95\%} & \textbf{94.12\%} & \textbf{83.22\%} & \textbf{85.49\%} & \textbf{68.84\%} & \textbf{69.88\%} & \textbf{66.36\%} \\ \hline

      \multicolumn{1}{|l|}{\multirow{2}{*}{ST-GCN++}} & Base & 84.78\% & 90.85\% & 76.54\% & 76.40\% & 47.37\% & 47.96\% & 46.86\% \\ \cline{2-2}
      \multicolumn{1}{|l|}{} & \textbf{PoseOFF} & \textbf{88.88\%} & \textbf{93.70\%} & \textbf{82.36\%} & \textbf{85.00\%} & \textbf{58.27\%} & \textbf{60.13\%} & \textbf{58.61\%} \\ \hline
    \end{tabular}
    }
  \end{table*}
\end{center}

\section{Experiments}%
\label{sec:experiments}

\textbf{Evaluation Metrics.}
For the task of action classification, model performance was measured using the classification accuracy; that is, the percentage of samples that the models were able predict the correct class label of after observing the full action sequence.
For the task of action prediction, both the classification accuracy at each given observation ratio and the Area Under the Curve (AUC) are reported.
The AUC is a measure of the model's performance across all evaluated observation ratios, and is calculated as:

\[ \text{AUC}=\frac{\sum{\text{Acc}}_{\text{obs}}}{\text{N}_{\text{obs}}}\]
where $\text{Acc}_{\text{obs}}$ is the accuracy at a given observation ratio, $\text{obs}$, and $\text{N}_{\text{obs}}$ is the number of evaluated observation ratios.


\subsection{Action Classification}%
\label{subsec:exp_action_classification}
% % How the results were obtained
To test the models' ability to perform action classification, all models - both the base model and a version of the model with the additional PoseOFF embedding layer, shown in~\autoref{fig:embedding_network_diagram} - were trained and tested on full sequences.
In the full sequence evaluation, no temporal masking was applied to the input sequence.
This test simulates an offline action recognition scenario.
% The PoseOFF data was learned using a simple CNN layer added to each model's embedding layer.
% The flow windows for the NTU-RGB+D and NTU-RGB+D120 models were extracted using dilation 3.
% The flow windows for the UCF101 dataset were extracted using dilation 1.


% Discussion of results table...
The action classification results are presented in \autoref{table:classification_accuracy}.
In this table, we have highlighted the best performing model for each dataset and evaluation.
The models trained with the additional PoseOFF embeddings produce a higher classification accuracy across all models and all datasets.
Across all models, those trained with the additional PoseOFF embeddings showed an average increase in classification of 2.14\%, 6.84\% and 11.22\% for the NTU RGB+D 60, NTU RGB+D 120 and UCF101 datasets respectively.


% The UCF101 dataset's results
Based on the results presented in the \autoref{table:classification_accuracy}, the UCF101 dataset shows the highest average increase in classification accuracy across all models and all evaluations.
This dataset is RGB only, containing human actions from videos ``in the wild''; no depth data or skeleton poses accompany the dataset.
The poses extracted using YOLO-POSE Large are generally less accurate or precise than those included in the NTU RGB+D datasets~\cite{maji2022YOLOPoseEnhancingYOLOMultiPersonPoseEstimationUsingObjectKeypointSimilarityLoss}\cite{shahroudy2016NTURGB+DLargeScaleDataset3DHumanActivityAnalysis}.
The significant increase in classification accuracy provided by PoseOFF embeddings for this dataset highlights the versatility of this feature embedding for real-world contexts.


\subsection{Action Prediction}%
\label{subsec:exp_action_prediction}
% How we tested action prediction
These experiments were designed to test whether the early visual motion cues extracted though PoseOFF facilitate earlier prediction of actions at a similar accuracy level to baseline models.
All models were trained using 100\% observation ratio and tested by viewing each video sequence at increments of 10\% observation ratios.
% To test the effectiveness of the learnable features present in PoseOFF data, we evaluated each model at different observation ratios of the input sequence.
% The models evaluated were trained on 100\% observation ratio and were evaluated at 10\% observation ratio intervals.
The InfoGCN++ model produces an action class prediction for every frame of the input sequence (64 frame input), so the observation ratio interval evaluations were not necessary for this model.
These experiments were conducted using the Cross Subject (CS) evaluation of the NTU RGB+D 60 dataset, and the second evaluation defined by the authors of the UCF101 dataset.
The action prediction results are presented in \autoref{fig:res_obs_ratio_ntu} and~\autoref{fig:res_obs_ratio_ucf}.


% Observation accuracy graphs
\begin{figure*}[]
  \centering
  \includegraphics[width=1\linewidth]{figures/NTU_RGBD_60-CS_obs_ratios.png}
  \caption{\label{fig:res_obs_ratio_ntu}Models trained using 100\% observation ratios, evaluated at 10\% observation ratio intervals on the NTU RGB+D 60 dataset, Cross Subject evaluation. The Area Under the Curve (AUC) is reported for each model in the sub figure labels.}
\end{figure*}

\begin{figure*}[]
  \centering
  \includegraphics[width=1\linewidth]{figures/UCF101-2_obs_ratios.png}
  \caption{\label{fig:res_obs_ratio_ucf}Models trained using 100\% observation ratios, evaluated at 10\% observation ratio intervals on the UCF101 evaluation 2. The Area Under the Curve (AUC) is reported for each model in the sub figure labels.}
\end{figure*}


% How do we interpret these graphs?
The results are the average performance of the model's as they ``see'' more of each action sequence.
In \autoref{fig:res_obs_ratio_ntu} and \autoref{fig:res_obs_ratio_ucf}, each point along the blue and orange curves corresponds to the action classification accuracy (y-axis) of the baseline and PoseOFF-embedded models, respectively, for a given observation ratio (x-axis).
The gray dotted lines in these graphs indicates the highest performance of the base model, in order to compare it's performance with the PoseOFF embedded versions of the models.


% NTU dataset results
The action prediction results on the NTU RGB+D 60 dataset, shown in~\autoref{fig:res_obs_ratio_ntu}, show that all models are able to predict the action taking place with fewer frames.
For the ST-GCN++ and MS-G3D models, the PoseOFF embedded version of the model only needs to observe 80\% of the entire action sequence to achieve the same performance as the base model at 100\% observation ratio.
At lower observation ratios, the ST-GCN++ base model is able to predict the action more accurately than the PoseOFF embedded model.
For InfoGCN++, which was specifically designed for the task of action prediction, the PoseOFF embedded version of the model needs to only see approximately half of the input sequence to achieve the same performance as the base model at 100\% observation ratio.

% UCF action prediction results
The action prediction results on the UCF101 dataset, shown in~\autoref{fig:res_obs_ratio_ucf}, show again that PoseOFF embedded models require fewer frames to achieve the same action classification accuracy as the base model with a full observation of the sequence.
For all models, PoseOFF features enable the model to predict the action with the same accuracy as the base model after only seeing half of the full sequence.


% Why is this important?
The changes to the skeleton action recognition models were confined to the first layers of the network and did not substantially affect the models’ internal operations.
We believe that the data produced from the PoseOFF extraction method can be easily implemented in any skeleton-based action recognition model to improve the model's action recognition and prediction ability.

% Existing skeleton-based action recognition models can be easily modified in order to learn features extracted with PoseOFF.
% This can improve the model performance for both human action classification and action prediction.

% What do the graphs show us?
% The action anticipation results are an indication that the embedding of the features extracted with the PoseOFF method are able to anticipate the action happening in the videos more accurately, and with fewer frames

\section{Conclusion}
\label{sec:conclusion}
We have demonstrated that the embedding of human early visual motion cues into established skeleton based human action recognition can increase overall action classification accuracy, and reduce number of frames required to achieve a competitive action classification accuracy.

The implementation of PoseOFF into existing skeleton-based action recognition models requires only a minor modification to the model's embedding layer.
By comparison to a two stream approach, using full optical flow frames as well as skeleton keypoints, PoseOFF presents a more data efficient method of extracting relevant human early visual motion cue information.

% Future work
In future work, we look to explore how the accuracy of the optical flow method choice impacts the benefits that PoseOFF provides.
If a faster, and perhaps less accurate method may be applied with little impact on demonstrated benefits, this would alleviate concerns that optical flow is too computationally expensive for real-time applications.
Additionally, future work may probe whether certain actions are more easily classified using these PoseOFF, which may offer further insights as to when it is most likely to offer benefits.


Showing that a fast and perhaps less accurate method can be applied with little impact on the demonstrated benefits would alleviate concerns that optical flow is too computationally expensive for real-time.
Future work could investigate whether PoseOFF preferentially improves classification of specific action types, providing insight into when it is most beneficial.
% In this work, PoseOFF was extracted with a fixed optical flow window sampling dilation. For the NTU-RGB+D and NTU-RGB+D 120 datasets, this flow sampling dilation was fixed to 3 pixels ($d=3$), and for the UCF101 dataset, there was no dilation ($d=1$).
% In future work, we will explore the effects of a dynamic sampling dilation.
% This dilation factor could vary with quantities such as the apparent size of the human skeleton with respect to the frame size, or a skeleton keypoint's distance from the camera.


\printbibliography

\end{document}
